---
import Layout from "../layouts/Layout.astro";
import { House, ArrowLeft } from "@lucide/astro";

interface Props {
  error?: unknown;
}

const { error } = Astro.props;

// Sanitize error for display - NEVER expose sensitive server details in production
const displayMessage =
  "Ocorreu um erro inesperado ao processar sua solicitação.";

// Log full error server-side for debugging (only visible in server logs)
if (error instanceof Error) {
  console.error("500 Server Error:", {
    message: error.message,
    stack: error.stack,
    timestamp: new Date().toISOString(),
  });
}
---

<Layout
  title="500 - Erro do servidor"
  description="Ocorreu um erro inesperado ao processar sua solicitação."
>
  <div class="min-h-screen flex items-center justify-center px-4 py-20">
    <div class="text-center max-w-2xl mx-auto">
      <!-- Large 500 Number -->
      <h1 class="text-9xl font-bold text-error mb-4 animate-pulse">500</h1>

      <!-- Error Title -->
      <h2 class="text-4xl font-bold mb-4">Erro do Servidor</h2>

      <!-- Error Description -->
      <p class="text-xl mb-8 text-base-content/70">
        {displayMessage}
        <br />
        Tente novamente mais tarde ou entre em contato com o suporte se o problema persistir.
      </p>

      <!-- Divider -->
      <div class="divider"></div>

      <!-- Action Buttons -->
      <div class="flex gap-4 justify-center flex-wrap mt-8">
        <a href="/" class="btn btn-primary gap-2">
          <House class="size-5" />
          Ir para a página inicial
        </a>
        <button onclick="history.back()" class="btn btn-outline gap-2">
          <ArrowLeft class="size-5" />
          Voltar
        </button>
      </div>

      <!-- Note about SSR -->
      <div class="mt-12 p-4 bg-base-200 rounded-lg">
        <p class="text-sm text-base-content/60">
          <strong>Nota:</strong> Esta página de erro só está ativa ao usar a Renderização do Lado do Servidor (SSR). Atualmente, o Bloomfolio está configurado para geração estática.
        </p>
      </div>

      <!-- Helpful Links -->
      <div class="mt-8">
        <p class="text-sm text-base-content/60 mb-4">Precisa de ajuda?</p>
        <div class="flex gap-3 justify-center flex-wrap text-sm">
          <a href="/#contact" class="link link-hover">Contato</a>
          <span class="text-base-content/30">•</span>
          <a
            href="https://github.com/lauroguedes/bloomfolio/issues"
            target="_blank"
            rel="noopener noreferrer"
            class="link link-hover">Relatar Problema</a
          >
        </div>
      </div>
    </div>
  </div>
</Layout>
